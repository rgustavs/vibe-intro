```html
<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover, user-scalable=no" />
<meta name="apple-mobile-web-app-capable" content="yes" />
<meta name="apple-mobile-web-app-status-bar-style" content="default" />
<title>The Vibe-Coded Stack · A working introduction</title>

<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,300;9..144,400;9..144,500&family=Inter:wght@400;500;600&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">

<style>
/* ---------- design tokens ---------- */
:root {
  --color-background: #F7F4EE;
  --color-surface: #FFFFFF;
  --color-panel: #F2F2EF;
  --color-text: #22201D;
  --color-subtext: #5A5F5B;
  --color-border: #E3E6E1;
  --color-accent-pine: #6F7F6A;
  --color-accent-fjord: #8FA9B8;
  --color-mist: #A8BDB6;
  --color-clay: #C9BAA6;
  --color-sand: #E6E2D8;
  --color-ink: #22201D;

  --radius-sm: 8px;
  --radius-md: 12px;
  --radius-lg: 18px;
  --radius-pill: 999px;

  --space-xs: 4px;
  --space-sm: 8px;
  --space-md: 16px;
  --space-lg: 24px;
  --space-xl: 40px;
  --space-2xl: 64px;

  --font-display: 'Fraunces', Georgia, 'Times New Roman', serif;
  --font-ui: 'Inter', system-ui, -apple-system, sans-serif;
  --font-code: 'JetBrains Mono', 'SF Mono', Consolas, monospace;
}

* { box-sizing: border-box; margin: 0; padding: 0; }

html, body {
  height: 100%;
  overflow: hidden;
  -webkit-tap-highlight-color: transparent;
  -webkit-touch-callout: none;
  -webkit-user-select: none;
  user-select: none;
}

body {
  background: var(--color-background);
  color: var(--color-text);
  font-family: var(--font-ui);
  font-size: 16px;
  line-height: 1.55;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  font-feature-settings: 'ss01', 'cv11';
}

/* ---------- structural ---------- */
.deck {
  position: relative;
  width: 100vw;
  height: 100vh;
  height: 100dvh;
  overflow: hidden;
  touch-action: pan-y;
}

.progress {
  position: absolute;
  top: 0; left: 0;
  height: 2px;
  background: var(--color-accent-pine);
  width: 0;
  transition: width 380ms cubic-bezier(.4,0,.2,1);
  z-index: 10;
  opacity: .85;
}

.brandmark {
  position: absolute;
  top: var(--space-lg);
  left: var(--space-xl);
  display: flex;
  align-items: center;
  gap: 10px;
  font-family: var(--font-code);
  font-size: 11px;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--color-subtext);
  z-index: 5;
}
.brandmark svg { display: block; }

.counter {
  position: absolute;
  top: var(--space-lg);
  right: var(--space-xl);
  font-family: var(--font-code);
  font-size: 11px;
  letter-spacing: 0.12em;
  color: var(--color-subtext);
  z-index: 5;
  font-variant-numeric: tabular-nums;
}

/* ---------- slides ---------- */
.slide {
  position: absolute;
  inset: 0;
  display: flex;
  flex-direction: column;
  justify-content: center;
  padding: clamp(48px, 7vw, 96px) clamp(28px, 7vw, 96px);
  opacity: 0;
  visibility: hidden;
  transform: translateY(12px);
  transition: opacity 420ms cubic-bezier(.4,0,.2,1), transform 420ms cubic-bezier(.4,0,.2,1), visibility 0s linear 420ms;
}
.slide.is-active {
  opacity: 1;
  visibility: visible;
  transform: translateY(0);
  transition: opacity 520ms cubic-bezier(.4,0,.2,1), transform 520ms cubic-bezier(.4,0,.2,1), visibility 0s;
}

.slide.is-active .reveal {
  animation: rise 700ms cubic-bezier(.2,.7,.2,1) both;
}
.slide.is-active .reveal:nth-child(1) { animation-delay: 60ms; }
.slide.is-active .reveal:nth-child(2) { animation-delay: 160ms; }
.slide.is-active .reveal:nth-child(3) { animation-delay: 260ms; }
.slide.is-active .reveal:nth-child(4) { animation-delay: 360ms; }
.slide.is-active .reveal:nth-child(5) { animation-delay: 460ms; }
.slide.is-active .reveal:nth-child(6) { animation-delay: 540ms; }
.slide.is-active .reveal:nth-child(7) { animation-delay: 620ms; }
@keyframes rise {
  from { opacity: 0; transform: translateY(8px); }
  to   { opacity: 1; transform: translateY(0); }
}

/* ---------- typography ---------- */
.eyebrow {
  font-family: var(--font-code);
  font-size: 11px;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: var(--color-accent-pine);
  margin-bottom: var(--space-md);
}
h1.display {
  font-family: var(--font-display);
  font-weight: 400;
  font-size: clamp(40px, 7.5vw, 80px);
  line-height: 1.02;
  letter-spacing: -0.02em;
  color: var(--color-text);
  font-variation-settings: "opsz" 144, "SOFT" 50;
}
h1.display em {
  font-style: italic;
  color: var(--color-accent-pine);
  font-weight: 300;
}
h2.heading {
  font-family: var(--font-display);
  font-weight: 400;
  font-size: clamp(28px, 4.6vw, 52px);
  line-height: 1.08;
  letter-spacing: -0.015em;
  color: var(--color-text);
  margin-bottom: var(--space-lg);
}
h2.heading em {
  font-style: italic;
  color: var(--color-accent-pine);
  font-weight: 300;
}
.lede {
  font-family: var(--font-display);
  font-weight: 300;
  font-style: italic;
  font-size: clamp(20px, 2.4vw, 28px);
  line-height: 1.4;
  color: var(--color-subtext);
  max-width: 62ch;
}
.body {
  font-size: clamp(15px, 1.4vw, 18px);
  line-height: 1.6;
  color: var(--color-text);
  max-width: 62ch;
}
.subtext { color: var(--color-subtext); }

/* ---------- composition ---------- */
.stack > * + * { margin-top: var(--space-lg); }
.stack-sm > * + * { margin-top: var(--space-sm); }
.grid-2 {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: var(--space-lg);
}
@media (max-width: 760px) { .grid-2 { grid-template-columns: 1fr; } }

.card {
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: clamp(20px, 2.4vw, 32px);
}
.card .label {
  font-family: var(--font-code);
  font-size: 10.5px;
  letter-spacing: 0.16em;
  text-transform: uppercase;
  color: var(--color-accent-pine);
  margin-bottom: 8px;
}
.card .title {
  font-family: var(--font-display);
  font-size: clamp(20px, 2vw, 26px);
  font-weight: 400;
  letter-spacing: -0.01em;
  line-height: 1.15;
  margin-bottom: 10px;
  color: var(--color-text);
}
.card p { color: var(--color-subtext); font-size: 15px; line-height: 1.55; }

.prompt-block {
  background: #F7F7F4;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  padding: 18px 20px;
  font-family: var(--font-code);
  font-size: 13.5px;
  line-height: 1.65;
  color: var(--color-text);
  white-space: pre-wrap;
  position: relative;
}
.prompt-block .tag {
  position: absolute;
  top: 10px; right: 14px;
  font-size: 10px;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--color-subtext);
}
.prompt-block.good .tag { color: var(--color-accent-pine); }
.prompt-block .k { color: var(--color-accent-pine); }
.prompt-block .c { color: var(--color-subtext); font-style: italic; }
.prompt-block .arr { color: var(--color-accent-fjord); }

/* ---------- title slide ---------- */
.title-slide { align-items: flex-start; }
.title-mark { margin-bottom: var(--space-xl); }
.title-meta {
  margin-top: var(--space-2xl);
  display: flex;
  gap: var(--space-xl);
  flex-wrap: wrap;
  font-family: var(--font-code);
  font-size: 11px;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--color-subtext);
}
.title-meta span strong {
  color: var(--color-text);
  font-weight: 500;
  margin-left: 6px;
}

/* ---------- anatomy diagram ---------- */
.anatomy {
  display: grid;
  grid-template-columns: 1.1fr 1fr;
  gap: var(--space-xl);
  align-items: center;
}
@media (max-width: 880px) { .anatomy { grid-template-columns: 1fr; } }
.anatomy-stack { display: flex; flex-direction: column; gap: 8px; }
.anatomy-row {
  display: grid;
  grid-template-columns: 110px 1fr;
  gap: var(--space-md);
  align-items: center;
  padding: 14px 18px;
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
}
.anatomy-row .pill {
  font-family: var(--font-code);
  font-size: 10px;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--color-accent-pine);
  background: #EEF1EA;
  padding: 4px 8px;
  border-radius: var(--radius-pill);
  text-align: center;
  width: max-content;
}
.anatomy-row.middle .pill { color: #8FA9B8; background: #EEF3F5; }
.anatomy-row.bottom .pill { color: #b07a52; background: #FAF6F2; }

.anatomy-row p { font-size: 14.5px; color: var(--color-text); }
.anatomy-row p span { color: var(--color-subtext); display: block; margin-top: 2px;}

/* ---------- takeaways ---------- */
.takeaways {
  display: grid;
  grid-template-columns: 80px 1fr;
  gap: var(--space-md) var(--space-lg);
  align-items: baseline;
  max-width: 760px;
}
.takeaways .num {
  font-family: var(--font-display);
  font-style: italic;
  font-weight: 300;
  font-size: 36px;
  color: var(--color-accent-pine);
  line-height: 1;
}
.takeaways .t {
  font-family: var(--font-display);
  font-size: clamp(20px, 2vw, 26px);
  font-weight: 400;
  line-height: 1.25;
  color: var(--color-text);
  padding-bottom: var(--space-md);
  border-bottom: 1px solid var(--color-border);
}
.takeaways > *:nth-last-child(-n+2) .t,
.takeaways > *:nth-last-child(-n+2).t { border-bottom: none; }

/* ---------- nav ---------- */
.nav {
  position: absolute;
  bottom: var(--space-lg);
  left: 50%;
  transform: translateX(-50%);
  display: flex;
  align-items: center;
  gap: 14px;
  z-index: 6;
  background: rgba(255,255,255,0.7);
  backdrop-filter: blur(8px);
  -webkit-backdrop-filter: blur(8px);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-pill);
  padding: 6px 8px;
}
.nav button {
  appearance: none;
  background: transparent;
  border: 0;
  width: 36px; height: 36px;
  border-radius: 50%;
  color: var(--color-text);
  cursor: pointer;
  display: grid;
  place-items: center;
  transition: background 180ms ease;
  -webkit-tap-highlight-color: transparent;
}
.nav button:hover { background: #EEF1EA; }
.nav button:active { background: #E3E6E1; }
.nav button[disabled] { opacity: 0.3; cursor: default; }
.nav button[disabled]:hover { background: transparent; }
.nav .dots { display: flex; gap: 6px; padding: 0 6px; }
.nav .dot {
  width: 6px; height: 6px;
  border-radius: 50%;
  background: var(--color-border);
  transition: background 220ms ease, transform 220ms ease;
}
.nav .dot.is-active {
  background: var(--color-accent-pine);
  transform: scale(1.25);
}

.tapzone {
  position: absolute;
  top: 0; bottom: 80px;
  width: 22%;
  z-index: 4;
  cursor: pointer;
  -webkit-tap-highlight-color: transparent;
}
.tapzone.left { left: 0; }
.tapzone.right { right: 0; }
.tapzone::after {
  content: '';
  position: absolute;
  top: 50%; transform: translateY(-50%);
  width: 44px; height: 44px;
  border-radius: 50%;
  opacity: 0;
  transition: opacity 200ms ease;
  background: rgba(255,255,255,0.6);
  border: 1px solid var(--color-border);
}
.tapzone.left::after  { left: 24px; }
.tapzone.right::after { right: 24px; }
@media (hover: hover) { .tapzone:hover::after { opacity: 1; } }

.hint {
  position: absolute;
  bottom: 92px;
  left: 50%;
  transform: translateX(-50%);
  font-family: var(--font-code);
  font-size: 10.5px;
  letter-spacing: 0.16em;
  text-transform: uppercase;
  color: var(--color-subtext);
  opacity: 0;
  animation: hintFade 4.5s 1.4s ease forwards;
  pointer-events: none;
  white-space: nowrap;
  z-index: 5;
}
@keyframes hintFade {
  0%   { opacity: 0; transform: translate(-50%, 6px); }
  18%  { opacity: 0.9; transform: translate(-50%, 0); }
  78%  { opacity: 0.9; }
  100% { opacity: 0; }
}

/* ---------- example slides ---------- */
.slide.example {
  padding-top: clamp(64px, 8vh, 96px);
  padding-bottom: clamp(80px, 10vh, 110px);
}
.example-grid {
  display: grid;
  grid-template-columns: 0.85fr 1.15fr;
  gap: clamp(20px, 3vw, 44px);
  align-items: stretch;
}
@media (max-width: 880px) { .example-grid { grid-template-columns: 1fr; } }
.example-grid .lead { display: flex; flex-direction: column; justify-content: center; }
.example-grid .lead h3 {
  font-family: var(--font-display);
  font-weight: 400;
  font-size: clamp(22px, 2.6vw, 32px);
  line-height: 1.15;
  letter-spacing: -0.015em;
  color: var(--color-text);
  margin-bottom: 14px;
}
.example-grid .lead h3 em { font-style: italic; color: var(--color-accent-pine); }
.example-grid .lead p {
  color: var(--color-subtext);
  font-size: 14.5px;
  line-height: 1.55;
  margin-bottom: 10px;
}
.example-grid .lead ul.tags {
  list-style: none;
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
  margin-top: 12px;
  padding: 0;
}
.example-grid .lead ul.tags li {
  font-family: var(--font-code);
  font-size: 10px;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--color-accent-pine);
  background: #EEF1EA;
  padding: 4px 10px;
  border-radius: var(--radius-pill);
}
.example-grid .prompt-block {
  font-size: 12.5px;
  line-height: 1.62;
  max-height: 64vh;
  overflow: auto;
  -webkit-overflow-scrolling: touch;
  padding-top: 30px;
}
.example-grid .prompt-block .h {
  display: block;
  color: var(--color-accent-pine);
  font-weight: 500;
  letter-spacing: 0.08em;
  font-size: 10px;
  text-transform: uppercase;
  margin: 14px 0 4px;
}
.example-grid .prompt-block .h:first-child { margin-top: 0; }

@media (max-width: 900px) {
  .brandmark, .counter { top: 16px; }
  .brandmark { left: 22px; }
  .counter { right: 22px; }
  .anatomy { gap: var(--space-lg); }
}
</style>
</head>
<body>

<div class="deck" id="deck" role="region" aria-label="The Vibe Stack presentation">

  <div class="progress" id="progress"></div>

  <div class="brandmark" aria-hidden="true">
    <svg width="22" height="22" viewBox="0 0 22 22" fill="none">
      <rect x="4" y="14" width="14" height="4" rx="1" fill="#6F7F6A"/>
      <rect x="4" y="9" width="14" height="4" rx="1" fill="#8FA9B8"/>
      <rect x="4" y="4" width="14" height="4" rx="1" fill="#C9BAA6"/>
    </svg>
    <span>Vibe Stack · for PMs</span>
  </div>
  <div class="counter" id="counter">01 / 13</div>

  <!-- =================== SLIDE 1 · TITLE =================== -->
  <section class="slide title-slide is-active" data-slide="1">
    <div class="title-mark reveal" aria-hidden="true">
      <svg width="60" height="50" viewBox="0 0 60 50" fill="none">
        <rect x="4" y="34" width="52" height="12" rx="2" fill="#6F7F6A" opacity="0.9"/>
        <rect x="4" y="19" width="52" height="12" rx="2" fill="#8FA9B8" opacity="0.9"/>
        <rect x="4" y="4" width="52" height="12" rx="2" fill="#C9BAA6" opacity="0.9"/>
        <path d="M12 40 L16 40 M12 25 L16 25 M12 10 L16 10" stroke="#FFF" stroke-width="2" stroke-linecap="round"/>
      </svg>
    </div>
    <div class="eyebrow reveal">A working introduction · part four</div>
    <h1 class="display reveal">The Vibe-Coded<br/><em>Stack.</em></h1>
    <p class="lede reveal" style="margin-top: var(--space-lg);">How AI puts full-stack architecture together —<br/>Node.js, FastAPI, and SQLite.</p>
    <div class="title-meta reveal">
      <span>Audience <strong>Product Managers</strong></span>
      <span>Length <strong>13 slides</strong></span>
      <span>Reading <strong>~10 min</strong></span>
    </div>
  </section>

  <!-- =================== SLIDE 2 · WHY =================== -->
  <section class="slide" data-slide="2">
    <div class="eyebrow reveal">01 · Why this matters</div>
    <h2 class="heading reveal">From single scripts to <em>actual apps.</em></h2>
    <div class="grid-2 reveal" style="margin-top: var(--space-md);">
      <div class="stack">
        <p class="body">"Vibe coding" started as generating single-file Python scripts. But products require a user interface, a place to store data, and business logic connecting the two.</p>
        <p class="body subtext">To vibe-code a real product, you need to understand the architectural boundaries. AI is incredibly fast at writing code, but it still needs you to dictate the blueprint.</p>
      </div>
      <div class="card">
        <div class="label">The Monolith Myth</div>
        <div class="title">"Just put it in one file"</div>
        <p>Works for a 5-minute prototype. Fails when you need a real UI, secure API keys, and persistent data.</p>
        <div class="label" style="margin-top: 20px;">The Vibe Stack</div>
        <div class="title">Separation of Concerns</div>
        <p>A decoupled UI, an API backend, and a simple database. The sweet spot between prototype and enterprise.</p>
      </div>
    </div>
  </section>

  <!-- =================== SLIDE 3 · THE KNOWLEDGEBASE =================== -->
  <section class="slide" data-slide="3">
    <div class="eyebrow reveal">02 · The Scenario</div>
    <h2 class="heading reveal">Building a <em>Knowledgebase.</em></h2>
    <div class="grid-2 reveal">
      <p class="body">To anchor this, let's look at a classic internal tool: a simple knowledgebase. Users need to read articles, search for topics, and authors need to write them.</p>
      <div class="prompt-block good">
<span class="tag">requirements</span>
<span class="h">Core Features</span>1. View a list of articles.
2. Read a specific article.
3. Search by title or keyword.

<span class="h">The Problem</span>If this is just an HTML file,
where do the articles live?
How do you update them?
How do you search across them
without loading everything at once?
      </div>
    </div>
    <p class="lede reveal" style="margin-top: var(--space-lg);">You need a frontend to display it, a backend to search it, and a database to store it.</p>
  </section>

  <!-- =================== SLIDE 4 · ANATOMY =================== -->
  <section class="slide" data-slide="4">
    <div class="eyebrow reveal">03 · Architecture Anatomy</div>
    <h2 class="heading reveal">Three layers of a modern Vibe App.</h2>
    <div class="anatomy reveal">
      <div class="anatomy-stack">
        <div class="anatomy-row"><span class="pill">Node / React</span>  <p>The Frontend (UI/UX) <span>Runs in the user's browser. Fetches data, displays buttons, handles clicks.</span></p></div>
        <div class="anatomy-row middle"><span class="pill">FastAPI</span>   <p>The Backend (API) <span>Runs on a server. Holds secret keys, talks to AI models, processes data safely.</span></p></div>
        <div class="anatomy-row bottom"><span class="pill">SQLite</span>     <p>The Database (State) <span>Stores the actual articles. A simple file on the server.</span></p></div>
      </div>
      <p class="lede">AI knows this stack perfectly. If you ask for a "React, FastAPI, SQLite stack," it knows exactly how to wire them together.</p>
    </div>
  </section>

  <!-- =================== SLIDE 5 · THE FRONTEND =================== -->
  <section class="slide" data-slide="5">
    <div class="eyebrow reveal">Layer 1</div>
    <h2 class="heading reveal">The Frontend: <em>Node &amp; React.</em></h2>
    <div class="grid-2 reveal">
      <div class="stack">
        <p class="body">Why Node.js/React? Because LLMs are trained on millions of React examples. They generate beautiful Tailwind CSS components flawlessly.</p>
        <p class="body subtext">The frontend's only job is to look good and make HTTP requests (fetch) to the backend. It should contain <em>zero</em> business logic or database queries.</p>
      </div>
      <div class="card">
        <div class="label">For the Knowledgebase</div>
        <div class="title">Visuals &amp; State</div>
        <ul class="dotlist" style="margin-top: 8px;">
          <li>Search bar component</li>
          <li>Article list view</li>
          <li>Markdown rendering</li>
          <li class="muted">Loading spinners while fetching</li>
        </ul>
      </div>
    </div>
  </section>

  <!-- =================== SLIDE 6 · THE BACKEND =================== -->
  <section class="slide" data-slide="6">
    <div class="eyebrow reveal">Layer 2</div>
    <h2 class="heading reveal">The Backend: <em>FastAPI.</em></h2>
    <div class="grid-2 reveal">
      <div class="stack">
        <p class="body">Why FastAPI (Python)? Because Python is the undisputed lingua franca of AI. If you want to add RAG, embeddings, or agent logic later, the tools are in Python.</p>
        <p class="body subtext">FastAPI is lightweight, incredibly fast, and automatically documents itself (Swagger UI), which helps the AI agent understand the API it just wrote.</p>
      </div>
      <div class="card">
        <div class="label">For the Knowledgebase</div>
        <div class="title">Endpoints &amp; Logic</div>
        <ul class="dotlist" style="margin-top: 8px;">
          <li><span style="font-family: var(--font-code); font-size: 13px;">GET /articles</span></li>
          <li><span style="font-family: var(--font-code); font-size: 13px;">GET /articles/{id}</span></li>
          <li><span style="font-family: var(--font-code); font-size: 13px;">GET /search?q=query</span></li>
          <li class="muted">Connecting to SQLite safely</li>
        </ul>
      </div>
    </div>
  </section>

  <!-- =================== SLIDE 7 · THE DATABASE =================== -->
  <section class="slide" data-slide="7">
    <div class="eyebrow reveal">Layer 3</div>
    <h2 class="heading reveal">The Database: <em>SQLite.</em></h2>
    <div class="grid-2 reveal">
      <div class="stack">
        <p class="body">Why SQLite? Because you don't need a massive Postgres cluster to test an idea. SQLite is a database contained entirely within a single local file (<span style="font-family: var(--font-code);">app.db</span>).</p>
        <p class="body subtext">It requires zero setup, zero passwords, and zero network configuration. It is the ultimate database for vibe-coding a V1.</p>
      </div>
      <div class="card">
        <div class="label">For the Knowledgebase</div>
        <div class="title">Persistent Storage</div>
        <ul class="dotlist" style="margin-top: 8px;">
          <li>A table for `articles`</li>
          <li>Columns for `title`, `content`, `date`</li>
          <li>Retains data when the server restarts</li>
          <li class="muted">Easy to backup (just copy the file)</li>
        </ul>
      </div>
    </div>
  </section>

  <!-- =================== SLIDE 8 · PITFALLS =================== -->
  <section class="slide" data-slide="8">
    <div class="eyebrow reveal">04 · Pitfalls</div>
    <h2 class="heading reveal">Where the vibe <em>breaks.</em></h2>
    <div class="grid-2 reveal">
      <div class="stack-sm">
        <div class="card">
          <div class="title" style="font-size: 19px;">CORS Errors</div>
          <p>The #1 vibe killer. The React frontend (port 3000) tries to call FastAPI (port 8000), and the browser blocks it. Tell the AI: "Configure CORS on the FastAPI app."</p>
        </div>
        <div class="card">
          <div class="title" style="font-size: 19px;">Premature optimization</div>
          <p>Asking the AI for Redis caching, Docker Swarm, and Postgres on day one. Start with SQLite. Upgrade when it breaks.</p>
        </div>
      </div>
      <div class="stack-sm">
        <div class="card">
          <div class="title" style="font-size: 19px;">Secret leaks</div>
          <p>Putting API keys in the React code. The frontend runs on the user's machine. Keys go in the FastAPI backend only.</p>
        </div>
        <div class="card">
          <div class="title" style="font-size: 19px;">Database Locks</div>
          <p>SQLite doesn't love 10,000 users writing at the same exact millisecond. It's fine for reading and typical MVP traffic.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- =================== SLIDE 9 · EXAMPLE 1 · SQLITE =================== -->
  <section class="slide example" data-slide="9">
    <div class="eyebrow reveal">Worked example · 01 of 04</div>
    <h2 class="heading reveal" style="font-size: clamp(24px, 3.4vw, 40px); margin-bottom: var(--space-md);">The <em>Memory</em> (SQLite).</h2>
    <div class="example-grid reveal">
      <div class="lead">
        <h3>Setting up the schema.</h3>
        <p>The foundation of the app. You tell the agent what data you need to store, and it writes the SQL to structure that single `.db` file.</p>
        <p>Notice how simple it is. No external services required.</p>
        <ul class="tags">
          <li>Database</li><li>Schema</li><li>Local File</li>
        </ul>
      </div>
      <div class="prompt-block good">
<span class="tag">database.py</span>
<span class="c"># Generated by AI for the backend</span>
<span class="k">import</span> sqlite3

<span class="k">def</span> init_db():
    conn = sqlite3.connect(<span class="arg">'kb.db'</span>)
    c = conn.cursor()
    
    <span class="c"># Create the articles table</span>
    c.execute(<span class="arg">'''</span>
<span class="arg">        CREATE TABLE IF NOT EXISTS articles (</span>
<span class="arg">            id INTEGER PRIMARY KEY AUTOINCREMENT,</span>
<span class="arg">            title TEXT NOT NULL,</span>
<span class="arg">            content TEXT NOT NULL,</span>
<span class="arg">            created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP</span>
<span class="arg">        )</span>
    <span class="arg">'''</span>)
    conn.commit()
    conn.close()
      </div>
    </div>
  </section>

  <!-- =================== SLIDE 10 · EXAMPLE 2 · FASTAPI =================== -->
  <section class="slide example" data-slide="10">
    <div class="eyebrow reveal">Worked example · 02 of 04</div>
    <h2 class="heading reveal" style="font-size: clamp(24px, 3.4vw, 40px); margin-bottom: var(--space-md);">The <em>Brain</em> (FastAPI).</h2>
    <div class="example-grid reveal">
      <div class="lead">
        <h3>Exposing the data securely.</h3>
        <p>The backend acts as a bouncer. It receives a request from the frontend, securely reads from the SQLite database, and returns clean JSON.</p>
        <p>FastAPI makes routing incredibly semantic and easy to read.</p>
        <ul class="tags">
          <li>Backend</li><li>Python</li><li>JSON API</li>
        </ul>
      </div>
      <div class="prompt-block good">
<span class="tag">main.py</span>
<span class="k">from</span> fastapi <span class="k">import</span> FastAPI
<span class="k">import</span> sqlite3

app = FastAPI()

<span class="c"># The Endpoint</span>
<span class="arr">@app.get</span>(<span class="arg">"/api/articles"</span>)
<span class="k">def</span> get_articles():
    conn = sqlite3.connect(<span class="arg">'kb.db'</span>)
    c = conn.cursor()
    
    <span class="c"># Query the database</span>
    c.execute(<span class="arg">"SELECT id, title FROM articles"</span>)
    articles = c.fetchall()
    conn.close()
    
    <span class="c"># Return JSON to the frontend</span>
    <span class="k">return</span> [{<span class="arg">"id"</span>: row[0], <span class="arg">"title"</span>: row[1]} <span class="k">for</span> row <span class="k">in</span> articles]
      </div>
    </div>
  </section>

  <!-- =================== SLIDE 11 · EXAMPLE 3 · REACT =================== -->
  <section class="slide example" data-slide="11">
    <div class="eyebrow reveal">Worked example · 03 of 04</div>
    <h2 class="heading reveal" style="font-size: clamp(24px, 3.4vw, 40px); margin-bottom: var(--space-md);">The <em>Face</em> (Node/React).</h2>
    <div class="example-grid reveal">
      <div class="lead">
        <h3>Fetching and displaying.</h3>
        <p>The UI doesn't know how the database works. It just calls the API URL, waits for the JSON, and paints the screen.</p>
        <p>This decoupling means you can change the DB later without rewriting the UI.</p>
        <ul class="tags">
          <li>Frontend</li><li>React</li><li>Fetch</li>
        </ul>
      </div>
      <div class="prompt-block good">
<span class="tag">App.jsx</span>
<span class="k">import</span> { useState, useEffect } <span class="k">from</span> <span class="arg">'react'</span>;

<span class="k">export default function</span> KnowledgeBase() {
  <span class="k">const</span> [articles, setArticles] = useState([]);

  useEffect(() <span class="arr">=></span> {
    <span class="c">// Call the FastAPI backend</span>
    <span class="k">fetch</span>(<span class="arg">'http://localhost:8000/api/articles'</span>)
      .then(res <span class="arr">=></span> res.json())
      .then(data <span class="arr">=></span> setArticles(data));
  }, []);

  <span class="k">return</span> (
    <span class="arr">&lt;div className="p-8"&gt;</span>
      <span class="arr">&lt;h1 className="text-2xl font-bold"&gt;</span>KB Articles<span class="arr">&lt;/h1&gt;</span>
      <span class="arr">&lt;ul&gt;</span>
        {articles.map(article <span class="arr">=></span> (
          <span class="arr">&lt;li key=</span>{article.id}<span class="arr">&gt;</span>{article.title}<span class="arr">&lt;/li&gt;</span>
        ))}
      <span class="arr">&lt;/ul&gt;</span>
    <span class="arr">&lt;/div&gt;</span>
  );
}
      </div>
    </div>
  </section>

  <!-- =================== SLIDE 12 · EXAMPLE 4 · THE PROMPT =================== -->
  <section class="slide example" data-slide="12">
    <div class="eyebrow reveal">Worked example · 04 of 04</div>
    <h2 class="heading reveal" style="font-size: clamp(24px, 3.4vw, 40px); margin-bottom: var(--space-md);">The <em>Architecture Prompt.</em></h2>
    <div class="example-grid reveal">
      <div class="lead">
        <h3>Telling the agent how to build it.</h3>
        <p>When you sit down with an AI coding assistant (like Cursor, Claude, or Antigravity), you don't ask it to "build an app."</p>
        <p>You explicitly define the architecture stack so it wires the pieces correctly from step one.</p>
        <ul class="tags">
          <li>System Prompt</li><li>Context</li><li>Architecture</li>
        </ul>
      </div>
      <div class="prompt-block good">
<span class="tag">vibe prompt</span>
<span class="h">Goal</span>Build a simple Knowledgebase app.

<span class="h">Architecture</span>- <span class="k">Frontend:</span> React (using Tailwind for styling).
- <span class="k">Backend:</span> FastAPI (Python).
- <span class="k">Database:</span> SQLite.

<span class="h">Instructions</span>1. Create the SQLite schema for `articles`
   (id, title, content).
2. Write a FastAPI endpoint to fetch all
   articles. ENABLE CORS so the frontend
   can access it locally.
3. Write a React component that fetches
   and displays the list.
4. Give me the terminal commands to run
   both servers concurrently.
      </div>
    </div>
  </section>

  <!-- =================== SLIDE 13 · TAKEAWAYS =================== -->
  <section class="slide" data-slide="13">
    <div class="eyebrow reveal">In closing</div>
    <h2 class="heading reveal">Four things to <em>remember.</em></h2>
    <div class="takeaways reveal">
      <div class="num">01</div><div class="t">A real app separates the UI (React), logic (FastAPI), and data (SQLite).</div>
      <div class="num">02</div><div class="t">FastAPI is the bridge. It connects the frontend to the AI ecosystem.</div>
      <div class="num">03</div><div class="t">Start with SQLite. Don't let the agent over-engineer your MVP database.</div>
      <div class="num">04</div><div class="t">Specify the architecture in your prompt so the AI wires it up correctly.</div>
    </div>
    <p class="lede reveal" style="margin-top: var(--space-2xl);">Decoupled systems. Fast execution.</p>
  </section>

  <!-- nav: arrows + dots -->
  <div class="nav" role="navigation" aria-label="slide controls">
    <button id="prev" aria-label="Previous slide">
      <svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M9 2L4 7l5 5" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg>
    </button>
    <div class="dots" id="dots" aria-hidden="true"></div>
    <button id="next" aria-label="Next slide">
      <svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M5 2l5 5-5 5" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/></svg>
    </button>
  </div>

  <div class="tapzone left" id="tap-left"  aria-hidden="true"></div>
  <div class="tapzone right" id="tap-right" aria-hidden="true"></div>

  <div class="hint" id="hint">Swipe or tap to navigate · arrow keys also work</div>

</div>

<script>
(function () {
  const slides   = Array.from(document.querySelectorAll('.slide'));
  const total    = slides.length;
  const counter  = document.getElementById('counter');
  const progress = document.getElementById('progress');
  const prevBtn  = document.getElementById('prev');
  const nextBtn  = document.getElementById('next');
  const dotsWrap = document.getElementById('dots');
  const tapL     = document.getElementById('tap-left');
  const tapR     = document.getElementById('tap-right');
  const deck     = document.getElementById('deck');

  let i = 0;
  let lock = false;
  const LOCK_MS = 420;

  slides.forEach((_, idx) => {
    const d = document.createElement('span');
    d.className = 'dot';
    d.dataset.index = idx;
    dotsWrap.appendChild(d);
  });
  const dots = Array.from(dotsWrap.children);

  function pad(n) { return String(n).padStart(2, '0'); }

  function render() {
    slides.forEach((s, idx) => s.classList.toggle('is-active', idx === i));
    dots.forEach((d, idx) => d.classList.toggle('is-active', idx === i));
    counter.textContent = pad(i + 1) + ' / ' + pad(total);
    progress.style.width = (((i + 1) / total) * 100) + '%';
    prevBtn.disabled = i === 0;
    nextBtn.disabled = i === total - 1;
  }

  function go(delta) {
    if (lock) return;
    const next = Math.max(0, Math.min(total - 1, i + delta));
    if (next === i) return;
    i = next;
    lock = true;
    render();
    setTimeout(() => { lock = false; }, LOCK_MS);
  }
  function goTo(idx) {
    if (lock || idx === i) return;
    i = Math.max(0, Math.min(total - 1, idx));
    lock = true;
    render();
    setTimeout(() => { lock = false; }, LOCK_MS);
  }

  prevBtn.addEventListener('click', () => go(-1));
  nextBtn.addEventListener('click', () => go(1));
  dots.forEach(d => d.addEventListener('click', () => goTo(parseInt(d.dataset.index, 10))));

  tapL.addEventListener('click', () => go(-1));
  tapR.addEventListener('click', () => go(1));

  window.addEventListener('keydown', (e) => {
    if (e.key === 'ArrowRight' || e.key === 'PageDown' || e.key === ' ' || e.key === 'Enter') {
      e.preventDefault(); go(1);
    } else if (e.key === 'ArrowLeft' || e.key === 'PageUp' || e.key === 'Backspace') {
      e.preventDefault(); go(-1);
    } else if (e.key === 'Home') {
      goTo(0);
    } else if (e.key === 'End') {
      goTo(total - 1);
    }
  });

  let touchStartX = 0, touchStartY = 0, touchStartT = 0, swiping = false;
  const THRESH_X = 50;
  const THRESH_T = 600;
  const MAX_Y    = 80;

  deck.addEventListener('touchstart', (e) => {
    if (e.touches.length !== 1) return;
    const t = e.touches[0];
    touchStartX = t.clientX;
    touchStartY = t.clientY;
    touchStartT = Date.now();
    swiping = true;
  }, { passive: true });

  deck.addEventListener('touchend', (e) => {
    if (!swiping) return;
    swiping = false;
    const t = e.changedTouches[0];
    const dx = t.clientX - touchStartX;
    const dy = t.clientY - touchStartY;
    const dt = Date.now() - touchStartT;
    if (dt > THRESH_T) return;
    if (Math.abs(dy) > MAX_Y) return;
    if (Math.abs(dx) < THRESH_X) return;
    go(dx < 0 ? 1 : -1);
  }, { passive: true });

  let pStartX = null;
  deck.addEventListener('pointerdown', (e) => {
    if (e.pointerType === 'mouse' && e.button !== 0) return;
    if (e.pointerType === 'touch') return;
    pStartX = e.clientX;
  });
  deck.addEventListener('pointerup', (e) => {
    if (pStartX == null) return;
    const dx = e.clientX - pStartX;
    pStartX = null;
    if (Math.abs(dx) >= 80) go(dx < 0 ? 1 : -1);
  });

  function readHash() {
    const m = (location.hash || '').match(/^#(\d+)$/);
    if (m) {
      const idx = Math.max(0, Math.min(total - 1, parseInt(m[1], 10) - 1));
      i = idx;
    }
  }
  readHash();
  window.addEventListener('hashchange', () => { readHash(); render(); });

  render();
})();
</script>

</body>
</html>

```
